# Generation pipeline config (TrainDataGeneration.py)

timeout: 120
parallel_questions: 50 # Number of concurrent questions sent to inference API
llm_max_retries: 3

prompts: prompts/prompts_blindguard.json # Prompt template file (JSON) used by agents, see example in ./prompts

dataset_tag: MA # Dataset selection (must match a class TAG in DatasetManager.py)
ma_dataset_path: MA/Task_generation/output/msmarco_contaminated_benchmark.json
questions_random_seed: 132 # Seed for question selection
verbose: false

# Output
save_data_dir: data # Directory to save generated data
file_name: MA-data.pkl # Name of data file (must be .pkl)

# Optional post-processing
process_text: true          # Turn text output in embeddings with TextProcessingManager.RoundProcessor
clean_data: true            # Drop debates with empty/invalid agent outputs (suggested to leave as true)
text_process_workers: 0     # 0 = auto; GPU processors will be forced to sequential

# Debate parameters
# Debate parameters
debate_config:
  num_agents: 8
  num_malicious: 0
  max_rounds: 3
  consensus_threshold: 1.0
  malicious_randomization_seed: 32

  # Question counts per topology
  n_questions: 200
  n_questions_random_topo: 200

  # Random topology generation
  random_topo_seed: 2446
  density: 
    min: 0.45
    max: 0.9
